var makeTree = function(){
  var newTree = {
    value: undefined;
    children: undefined;
  };

  return newTree;
};

var treeMethods = {};

treeMethods.addChild = function(){
};

treeMethods.contains = function(){
};
