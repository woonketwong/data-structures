var makeTree = function(value){
  var newTree = {
    value: value;
    children: undefined;
  };

  return newTree;
};

var treeMethods = {};

treeMethods.addChild = function(value){
};

treeMethods.contains = function(){
};
